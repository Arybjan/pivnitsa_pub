# --- Основной веб-фреймворк и сервер ---
fastapi==0.141.1               # Веб-фреймворк: маршрутизация, эндпоинты, внедрение зависимостей (Depends)
uvicorn==0.52.4                # Высокопроизводительный ASGI-сервер для запуска FastAPI
starlette==1.6.0               # Ядро FastAPI (обработка HTTP-запросов, фоновые задачи BackgroundTasks)

# --- База данных и миграции ---
SQLAlchemy==2.0.52             # ORM для работы с базой данных (модели, сессии, запросы)
asyncpg==0.31.0                # Асинхронный драйвер PostgreSQL для SQLAlchemy
alembic==1.19.1                # Управление и применение миграций схемы БД
greenlet==3.5.5                # Поддержка асинхронности в SQLAlchemy ORM

# --- Валидация данных и конфигурация ---
pydantic==2.13.4               # Валидация входных данных, DTO-схемы запросов и ответов
pydantic-settings==2.15.0      # Чтение и типизация настроек сервиса из .env (Settings)
pydantic_core==2.46.4          # C-ядро Pydantic для быстрой валидации данных
python-dotenv==1.2.3           # Загрузка переменных окружения из файла .env

# --- HTTP-клиент (SMS-шлюз) ---
httpx==0.28.1                  # Асинхронный HTTP-клиент для отправки XML-запросов к шлюзу Nikita SMS
httpcore==1.0.9                # Низкоуровневый движок HTTP-соединений для HTTPX

# --- Тестирование и симуляция сети ---
pytest==9.1.1                  # Фреймворк для написания и запуска автоматических тестов
pytest-asyncio==1.4.0          # Поддержка асинхронных тестов (async def test_*)
respx==0.23.1                  # Имитация сетевых сбоев, задержек и мокирование HTTPX-запросов
aiosqlite==0.22.1              # Асинхронная in-memory база данных SQLite для быстрых тестов

# --- Вспомогательные библиотеки ---
anyio==4.14.2                  # Асинхронный слой совместимости для FastAPI и AnyIO
annotated-doc==0.0.5           # Документирование аннотаций типов
annotated-types==0.8.0         # Ограничения и метаданные для Annotated типов
certifi==2026.7.22             # Корневые SSL-сертификаты для безопасных HTTPS-запросов
click==8.4.2                   # CLI-интерфейс для команд Uvicorn и Alembic
h11==0.16.0                    # Реализация протокола HTTP/1.1
httptools==0.8.0               # Быстрый парсер HTTP на C для Uvicorn
idna==3.19                     # Поддержка интернационализированных доменных имен
Mako==1.4.1                    # Шаблонизатор для генерации файлов миграций Alembic
MarkupSafe==3.0.3              # Безопасное экранирование строк для шаблонов Mako
PyYAML==6.0.3                  # Парсер YAML-файлов конфигурации
typing_extensions==4.16.0      # Расширенные возможности аннотаций типов Python
typing-inspection==0.4.4       # Инспекция типов для Pydantic и FastAPI
uvloop==0.22.1                 # Ускоренный event-loop на C для Linux
watchfiles==1.2.0              # Отслеживание изменений файлов при запуске с флагом --reload
websockets==17.0.1             # Поддержка протокола WebSockets для Uvicorn
